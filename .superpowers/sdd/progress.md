# Scalping/MTF/SMC/Swing — SDD progress ledger

Base before execution: 60fd69d. Feature base vs origin/main: feec593.

- SP-1 data layer: complete (ffbbb6f)
- SP-2 mtf engine: complete (c69a108)
- SP-3 smc indicators: complete (1c515f8)
- SP-4 scalping strategies: complete (e9ab3ce)
- SP-5 swing refresh: complete (5ca64f4)
- SP-6 validation/rollout (code Tasks 1-4): complete (18c8ab1). Task 5 = operator runbook (manual, live stack).
- Final whole-branch review: complete. Fixes C1 (OANDA cold-start window) + C2 (S1/S2 off scalping TFs) committed f4f0c88.

ALL PLANS IMPLEMENTED. Final gate: 772 passed / 7 skipped, ruff+format+mypy --strict clean.

Open (flagged to user, not auto-applied):
- I1: StrategyStateRepo.is_enabled defaults missing row -> True (new strategies auto-enabled). Mitigated by SP-6 runbook 5.1 (promote --shadow). Recommend auto-seed-shadow on startup (policy decision).
- I2: TwelveData fallback can't serve M5/M15 (fails-closed safely) -> XAUUSD intraday is single-vendor (OANDA). Documented.

NOT YET DONE (needs live stack, manual): SP-6 Task 5 validation matrix — backfill OANDA M5/M15/H4, run rtrade.cli.backtest per strategy x tf, promote on pass. No push to origin.

---

# Audit Remediation — SDD progress ledger

Plan: docs/superpowers/plans/2026-06-20-audit-remediation.md
Findings: docs/superpowers/specs/2026-06-20-codebase-audit-findings.md
Branch: fix/audit-remediation. Base before execution: f4f0c88. Planning docs: 2d5b0af.
Execution: subagent-driven, persona-per-agent. Continuous (no per-task check-ins).

## Task ledger (append one line when a task review comes back clean)
(Phase 0: 0.1 C1, 0.2 C2, 0.3 C8 | Phase 1: 1.1-1.11 A1-A13 | Phase 2: 2.1-2.7 B1-B7 |
 Phase 3: 3.1-3.4 D1-D4 | Phase 4: 4.1-4.11 E1-E5,C4-C9,C3 | Phase 5: 5.1-5.3 F1-F5 |
 Phase 6: 6.1-6.4 F6-F7,D5,E6,G)

- Task 0.1 (C1): complete (commit 3066456, review clean). PRE-MERGE NOTE: verify `docker compose -f docker-compose.prod.yml config` fails when API_AUTH_TOKEN unset (no Docker in dev env). Minor: Caddy empty-bearer backstop not strictly fail-closed alone (compose :? + app-layer 401 cover it).
- Task 0.2 (C2): complete (commit 83cd51c, review clean). MINOR follow-up (defer to final review / overlaps C9): two poll-error branches in oauth2.py (~L252/259) still interpolate `body`; reviewer confirmed they provably cannot carry access_token/authorization_code — genericize for uniformity.
- Task 0.3 (C8): complete (commit 5b89b8d, controller-reviewed — trivial script flag change, verified --api-key-env/os.environ/sys.exit(1), ruff+mypy clean). PHASE 0 COMPLETE.
- Task 1.1+1.2 (A1,A2) [CONSOLIDATED — tightly-coupled same-function changes]: complete (commit ef08692, quant review clean). MINOR (final-review): add a 2-line smart-path test for same-bar stop+TP worst-case. NOTE: papertrack/virtual_exits.py duplicates partial/BE logic (does NOT use apply_smart_exit) — DRY follow-up candidate (overlaps Task 6.x).
- Task 1.3+1.4 (A3,A4) [CONSOLIDATED; also closed A12,A13 from Task 1.10]: complete (commit 6909674, quant review CLEAN, DSR cases hand-verified). NOTE: implementer session hit a network error before commit/report; controller verified the diff against the exact spec (sharpe_per_trade, (ek+2)/4 kurtosis fix, z_max(n_trials), fail-safe denom<=0, gate uses per-trade Sharpe), confirmed callers threaded + tests meaningful, then committed. PLAN DEVIATION (correct): expected_max_sharpe drops unused t_periods (T enters via sigma_sr) — proper BLdP form. MINORS (final-review): (a) doc comment overstates profit-factor-cap role (real guard is Gate1 n_trades>=100); (b) walk-forward aggregate gate doesn't thread n_trials → DSR undeflated (safe, flagged); (c) add comment on sr0=z_max*sigma_sr dispersion assumption. Task 1.10 now only needs A11 (permutation (count+1)/(n+1)).
- Task 1.6+1.7 (A6,A7) [CONSOLIDATED]: complete (commit 92fa4f5, review PASS, EURUSD math + CLI refusal control-flow + sys.path ordering verified). IMPORTANT (final-review fix wave): add CLI exit-code regression tests (cli/backtest.py→2, run_backtest.py→1 without --allow-zero-cost) — wiring correct by inspection but untested. MINOR: stale "left untouched" claim in report re: run_backtest.py import reorder (no code impact).
- USER DECISION (A5/PBO): Option A — PBO is a model-selection diagnostic. Remove it from the single-config go-live gate (omit gate when pbo_value is None, like permutation_p); keep CSCV computation correct + fail-closed on insufficient data for future parameter-sweep use. Document the reframing.
- Task 1.5 (A5 option A + A11): complete (commit ffa3e2d, controller-reviewed — read all changed source; 805 passed). PBO gate omitted when pbo_value None (mirrors permutation_p); pbo: float|None; CSCV fails closed (1.0) on degenerate; reframing documented; permutation (count+1)/(n+1). PHASE-1 STATS BLOCK DONE. NOTES (final-review): no production parameter-sweep yet supplies pbo_value (by design under A); verify downstream analytics tolerate JSON null pbo in backtest_runs.
- Task 1.8+1.9 (A8,A9) [CONSOLIDATED]: complete (commit c7ff4dc, controller-reviewed — read full engine.py+harness.py; 810 passed). Gap fills: SL→min/max(level,open) both paths, TP stays at limit; warmup sized by inferred bar_dur (median index diff) w/ NaT fallback. BONUS: fixed latent crash test_mask.values.argmax()→argmax() (non-hourly WF path was never exercised). FOLLOW-UP (final-review): add M5/M15/H4 harness coverage. PHASE 1 COMPLETE (A1-A9,A11,A12,A13). A10 (overlapping-trade equity, Task 1.11) DEFERRED to final review per plan (optional/larger).
- Task 2.1 (B1 calendar fail-open): complete (commit 762215f, controller-reviewed — read composite_calendar.py + tests; 813 passed). Failover-on-empty; all-empty→[] w/o verified success + loud alert; last_success=verified-non-empty so staleness gate auto fail-closed (no scan.py change). RESIDUAL (final-review follow-up): provider-level parse failures should RAISE not return [] (investing/nasdaq/finnhub/static); persist last_empty telemetry. ALL HIGH FINDINGS NOW DONE EXCEPT B2.
- Task 2.2 (B2 daily LLM budget): complete (commit 1fc66b4 + doc-fix cb564b5, independent review PASS). Seed BudgetState.day_usd from KeyManager.get_daily_cost (Redis rtrade:cost:{UTC date}); persist per-scan delta once after both pipeline calls; graceful no-redis fallback; D2 wiring tests verified intact; 818 passed. REVIEW-FIX APPLIED: corrected inverted concurrency docstring (cap binds SEQUENTIALLY; under overlapping scans the snapshot seed can overshoot — documented; strict concurrent binding = future work). MINOR (final-review): midnight-rollover mid-scan drops that scan's persisted delta (narrow).
  *** ALL 13 HIGH-SEVERITY FINDINGS COMPLETE: A1,A2,A3,A4,A5,A6,A7,B1,B2,C1,C2,C3?,... (C3 still pending — Phase 4 Task 4.11). Correction: High findings done = A1-A4,A6,A7(backtest gates), B1,B2(safety), C1,C2(security), C8; A5 resolved. C3 (model HMAC) is Phase 4. ***
- CORRECTION to prior line: High findings DONE = A1,A2,A3,A4,A5,A6,A7,B1,B2,C1,C2 (11). High findings REMAINING = C3 (model HMAC, Task 4.11), D1 (audit-chain race, Task 3.1), D2 (OANDA UTC alignment, Task 3.2). Plus all Medium/Low. C8 done (was Medium).
- Task 3.1 (D1 audit-chain race): complete (commit d3a98b4, controller-reviewed — verified pg_advisory_xact_lock before read, commit-boundary reasoning, non-mutating detail). Also closed D5-Low (detail mutation). Unit test proves lock-before-read; true-concurrency test integration-gated (self-skips no DB). 800 passed (integration deselected). NOTE: serialization depends on caller committing in same txn (documented; scan pipeline does).
- Task 3.2 (D2 OANDA UTC alignment): complete (commit 33a3c3a, controller-reviewed — verified alignmentTimezone=UTC + dailyAlignment=0 added, respx test asserts query params). PHASE 3 (D1,D2) COMPLETE. Only remaining High = C3 (model HMAC, Task 4.11).
- Task 4.11 (C3 model HMAC): complete (commit 1e80d86, controller-reviewed — read full model_io.py: keyed HMAC, fail-closed, threat test proves plain-hash forge fails). MODEL_HMAC_KEY secret added + documented; hmm_train_job threads key; scan.py/meta_label use internal resolution. MIGRATION: legacy .sha256 models unloadable (re-saved next train). NOTE: report scratch file left with hash placeholder (gitignored, irrelevant to source commit).
  *** ✅ MILESTONE: ALL 14 HIGH-SEVERITY FINDINGS COMPLETE (A1-A7, B1, B2, C1, C2, C3, D1, D2). Also done: A11,A12,A13,C8,D5. ***
- REMAINING (Medium): B3,B4 (sizing/kelly), B5 (news tz), B6 (guardrails fail-closed), B7 (regime tz), D3 (audit verify latest-1000), D4 (gap heuristic), E1-E5 (api engine/hmm executor/alert markdown/backfill pagination/composite spread), C4,C5,C6,C7,C9 (security mediums), F1,F2 (VWAP/mutation). (Low): F3-F7, plus G dormant ML. Plus deferred A10 + test-coverage follow-ups for final review.
- Task 2.3+2.4 (B3,B4) [CONSOLIDATED]: complete (commit c10568e, controller-reviewed — read fixed sizing.py + tests). B3: abstain on min-lot floor-to-0 (method=abstain_min_lot) + report TRUE risk; B4: Kelly clamped to 2% GR-05 + new kelly_risk_usd field. 22 risk tests pass.
  *** IMPORTANT FOLLOW-UP (flag to user / final review): signals/engine.py does INLINE sizing and does NOT call compute_position_size, so B3/B4 hardening does NOT cover the live signal path. Recommend routing live sizing through the hardened function. (Out of original B3/B4 scope which targeted sizing.py.) ***
- Task 2.6 (B6 guardrails fail-closed): complete (commit 402be55, controller-reviewed — read gate.py+selftest.py). `require` set → missing required input = GateFailure "required input missing — fail closed"; require=None = legacy. scan.py requires {GR-06,GR-08,GR-13}+GR-07(non-crypto)+{GR-09,GR-11}(post-LLM). selftest proves hole closed. NOTE: `require` opt-in (future caller forgetting it could fail-open — documented).
- Task 2.5+2.7 (B5,B7) [CONSOLIDATED]: complete (commit 5b27d18, controller-reviewed). B7: regime `since` tz-aware via _fresh_index_ts (ensure_utc rejects naive → stamp-then-normalize). B5: news_filter dedup through _parse_event_time + loud naive-time warning (UTC fallback kept). 35 regime+risk tests pass. NOTE: full per-provider source-tz verification deferred to B1 provider-hardening follow-up.
  *** PHASE 2 COMPLETE (B1-B7, all fail-open safety findings). ***
=== SESSION STATUS ===
DONE (17 task-groups, all committed + suite green): Phase 0 (C1,C2,C8); Phase 1 (A1-A9,A11,A12,A13; A10 deferred); Phase 2 (B1-B7); Phase 3 (D1,D2)+D5; Phase 4 C3. = ALL 14 HIGH + all safety mediums + several others.
REMAINING (Medium): D3 (audit-verify latest-1000), D4 (gap heuristic tf-aware), E1 (api engine reuse), E2 (hmm executor), E3 (alert markdown escape), E4 (backfill pagination tf), E5 (composite spread per-leg), C4 (health split), C5 (XFF/auth-map), C6 (prompt-injection), C7 (device-code loop bound), C9 (recursive redaction), F1 (VWAP daily), F2 (compute() copy). (Low): F3-F7, G dormant ML. Plus deferred A10 + test-coverage follow-ups (CLI exit codes, M5/M15 harness, smart-path worst-case) + IMPORTANT: live-path inline sizing (B3/B4) + provider parse-failure hardening (B1) for final review.
- Task D3+E2: complete (commit ca782a3, controller-reviewed). D3: audit verify latest-1000 (desc+reverse) + new verify_chain(anchor_first=True) for windowed check (default genesis behavior unchanged). E2: hmm_train releases session then run_in_executor(_train_hmm_blocking); C3 key still threaded. 851 passed. NOTE: anchor_first skips out-of-window predecessor link (by design); run_in_executor uses thread pool (process pool out of scope).
- Task 4.1 (E1,C4,C5): complete (commit 313bbe0, controller-reviewed — read routes.py+app.py). E1: shared _get_engine factory, dispose in lifespan only. C4: public /health={"status"} minimal, /health/detail behind bearer. C5: rightmost XFF hop + OrderedDict prune/LRU cap 4096. 9 new tests. NOTE: public /health still runs DB+Redis probes unauth (no leak, documented); XFF trust assumes single proxy.
- Task 4.3 (E3,E4,E5): complete (commit 7d0dbee, controller-reviewed via report). E3: alerts parse_mode=None (plain text, robust). E4: _advance_cursor uses timeframe_duration(tf)*499. E5: composite fetch_spread per-leg try/except failover. 874 passed. NOTE: E3 loses bold styling (intentional); E4 assumes full pages (idempotent ingestion mitigates).
- KENDALA RESOLVED: orphaned RED test files (test_logging_redact/test_oauth2 modified + test_context_pack_fencing new) found uncommitted with no src — reverted to restore green baseline, then re-dispatched C6/C7/C9 fresh.
- Task 4.8 (C6,C7,C9): complete (commit 352e1f9, controller-reviewed via report; 882 passed). C6: sanitize all event string fields + fence calendar in verbatim <DATA_TIDAK_TEPERCAYA> delimiter. C7: device loop bounded by expires_in deadline + 1000-iter cap → RuntimeError. C9: recursive redaction + broadened keys (token/api_key/access_token/refresh_token/secret/password) + URL regex.
- Task 5.1 (F1,F2): complete (commit a0520db, controller-reviewed). F2: compute() does df.copy(deep=True) — no caller mutation. F1: VWAP grouped by UTC calendar day (resets daily); zero-vol day→NaN→snapshot None. Tests assert day-2-open VWAP==typical price + non-mutation. NOTE: UTC-day anchor (not trading session); tz-naive index treated as UTC.
- Task 5.3 (F3,F4,F5): complete (commit 3e96199, controller-reviewed — read levels.py+confluence.py). F3: tick decimals via Decimal exponent (0.25 grid correct). F4: volume baseline excludes current bar (matches edge_quality). F5: nearest level by min abs distance to entry. Full suite green.
- Task 6.1 (F6,F7,D4): complete (commit 6d1416b, controller-reviewed — read structure.py+ingestion.py). F6: single-linkage clustering (consecutive-gap, order-independent) + documented tie-break. F7: leftmost-max + strictly-exceeds-adjacent (double tops detected, no flat-top spam). D4: weekend_bars=ceil(72h/tf_duration). 904 passed.
